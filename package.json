{
  "name": "ts-scaffold",
  "version": "1.0.0",
  "description": "this is a typescript scaffold project build with gulp.",
  "main": "./build/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node ./build/main.js",
    "build": "tsc"
  },
  "author": "piccus peng",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "typescript": "^5.2.2"
  }
}